[providers]
providers = ["php"]

[phases.setup]
nixpkgs = ["php82", "php82Packages.composer"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan config:clear && php artisan route:cache && php artisan serve --host 0.0.0.0 --port $PORT"